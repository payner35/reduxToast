//still work in progress....
//check out the core reducers


const initialState = {

};

export function imageReducer(state = initialState, action = {}) {
    switch (action.type) {
        default:
            return state;
    }
}


export default imageReducer;